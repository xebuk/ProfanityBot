services:
  telegram-bot:
    build: .
    container_name: telegram-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./main.py:/app/main.py
      - ./textutil.py:/app/textutil.py
      - ./database.py:/app/database.py
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    env_file:
      - .env